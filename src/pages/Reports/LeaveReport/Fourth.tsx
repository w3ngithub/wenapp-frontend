import React from 'react'
import CommonQuarters from './Common'

function Fourth({fromDate, toDate}: {fromDate: any; toDate: any}) {
  return <CommonQuarters fromDate={fromDate} toDate={toDate} />
}

export default Fourth
